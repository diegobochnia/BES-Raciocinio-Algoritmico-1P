distancia = int(input("Digite a distância da sua viagem: "))
if distancia <= 200:
    valorFinal = distancia * 0.5
    print("O preço da passagem é de: R$ ", valorFinal)
else:
    valorFinal = distancia * 0.45
    print("O preço da passagem é de: R$ ", valorFinal)

# Estrutura condicional composta (if e else).
